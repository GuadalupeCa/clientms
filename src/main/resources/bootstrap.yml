spring:
  application:
    name: client-ms
  cloud:
    config:
      uri: http://localhost:8888

springdoc:
  api-docs:
    groups:
      enabled: true
    path: /swagger-doc/v3/api-docs
  swagger-ui:
    path: /swagger-doc/swagger-ui.html
